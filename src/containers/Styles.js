const app = {
  position: 'relative',
  width: '100%',
  height: '100%',
};

const page = {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    width: '800px',
    height: '100%',
    margin: '0 auto'
};

export default {app, page}
